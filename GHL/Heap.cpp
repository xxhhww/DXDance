#include "Heap.h"
#include "Tools/Assert.h"

namespace GHL {
	Heap::Heap(const Device* device, EResourceUsage usage)
	: mDevice(device) {

	}

	void Heap::SetDebugName(const std::string& name) {

	}
}